<!-- User Management Navigation Tab -->
<div class="flex flex-wrap gap-2 mb-6 border-b border-gray-200">
    <a href="@Url.Action("Index")" class="px-4 py-3 font-medium text-sm border-b-2 transition @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "border-blue-600 text-blue-600" : "border-transparent text-gray-600 hover:text-gray-900")">
        <i class="fas fa-list mr-2"></i>All Users
    </a>
    <a href="@Url.Action("Statistics")" class="px-4 py-3 font-medium text-sm border-b-2 transition @(ViewContext.RouteData.Values["action"]?.ToString() == "Statistics" ? "border-blue-600 text-blue-600" : "border-transparent text-gray-600 hover:text-gray-900")">
        <i class="fas fa-chart-line mr-2"></i>Statistics
    </a>
    <a href="#" class="px-4 py-3 font-medium text-sm border-b-2 transition border-transparent text-gray-600 hover:text-gray-900">
        <i class="fas fa-user-tag mr-2"></i>Roles & Permissions
    </a>
</div>

<!-- Quick Links -->
<div class="mb-6">
    <div class="flex flex-wrap gap-2">
        <a href="@Url.Action("Index")" class="inline-flex items-center gap-2 px-4 py-2 border-2 border-blue-600 text-blue-600 rounded-lg hover:bg-blue-50 transition text-sm font-medium">
            <i class="fas fa-users"></i>All Users
        </a>
        <a href="@Url.Action("ByRole", new { role = "Client" })" class="inline-flex items-center gap-2 px-4 py-2 border-2 border-cyan-600 text-cyan-600 rounded-lg hover:bg-cyan-50 transition text-sm font-medium">
            <i class="fas fa-user-circle"></i>Clients
        </a>
        <a href="@Url.Action("ByRole", new { role = "Trainer" })" class="inline-flex items-center gap-2 px-4 py-2 border-2 border-amber-600 text-amber-600 rounded-lg hover:bg-amber-50 transition text-sm font-medium">
            <i class="fas fa-dumbbell"></i>Trainers
        </a>
        <a href="@Url.Action("ByRole", new { role = "Admin" })" class="inline-flex items-center gap-2 px-4 py-2 border-2 border-red-600 text-red-600 rounded-lg hover:bg-red-50 transition text-sm font-medium">
            <i class="fas fa-shield-alt"></i>Admins
        </a>
        <a href="@Url.Action("Statistics")" class="inline-flex items-center gap-2 px-4 py-2 border-2 border-green-600 text-green-600 rounded-lg hover:bg-green-50 transition text-sm font-medium">
            <i class="fas fa-chart-bar"></i>Statistics
        </a>
    </div>
</div>
